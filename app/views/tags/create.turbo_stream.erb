<%= turbo_stream.append "tags", partial: "tags/tag", locals: { tag: @tag } %>

<%= turbo_stream.replace "modal" do %>
  <turbo-frame id="modal"></turbo-frame>
<% end %>

<%= turbo_stream.replace "flash", partial: "shared/flash_messages" %>