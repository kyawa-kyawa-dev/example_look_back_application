<div class="max-w-5xl mx-auto">
  <!-- ページタイトル -->
  <div class="mb-6">
    <h2 class="text-2xl font-bold text-gray-900">新しいナレッジを追加</h2>
    <p class="text-gray-600 mt-1">学んだことを記録して、知識を蓄積しましょう</p>
  </div>

  <!-- フォーム -->
  <div class="bg-white rounded-lg border border-gray-200 p-6">
    <%= form_with model: @knowledge do |f| %>
      <div class="space-y-5">
        <!-- タイトル -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">
            タイトル<span class="text-red-500 ml-1">*</span>
          </label>
          <%= f.text_field :title, 
              placeholder: "例: Railsのルーティング設定方法", 
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent" %>
        </div>

        <!-- 内容 -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">
            内容
          </label>
          <%= f.text_area :body, 
              rows: 8,
              placeholder: "学んだことを記録してください...\n\n例:\nnamespace :admin do\n  resources :users\nend\n\nでネストした構造を作成できる",
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent font-mono text-sm" %>
          <p class="text-xs text-gray-500 mt-2">コードやメモを自由に記録できます</p>
        </div>

        <!-- 参考URL（複数） -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">
            参考URL（任意）
          </label>
          <div class="space-y-3" data-controller="url" data-url-target="container">
            <div class="flex gap-2" data-url-target="url">
              <%= f.text_field :urls,
                  type: "url",
                  placeholder: "https://example.com",
                  multiple: true,
                  name: "knowledge[urls][]",
                  class: "flex-1 px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent" %>
            </div>
            <button type="button" class="flex items-center gap-2 text-indigo-600 hover:text-indigo-700 hover:cursor-pointer font-medium text-sm" data-url-target="button" data-action="click->url#add">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
              </svg>
              さらに追加
            </button>
          </div>
          <p class="text-xs text-gray-500 mt-2">参考にした記事やドキュメントのURLを保存できます</p>
        </div>

        <!-- タグ選択 -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-3">
            タグ<span class="text-red-500 ml-1">*</span>
          </label>
          <div class="space-y-3">
            <div class="flex flex-wrap gap-2">
              <%= render partial: "tag", collection: @tags %>
            </div>
          </div>
        </div>

        <!-- 通知設定 -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-3">
            復習通知
          </label>
          <div class="space-y-3">
            <div class="flex items-start">
              <%= f.check_box :notify_3days, class: "w-4 h-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500 mt-1" %>
              <%= f.label :notify_3days, class: "ml-3 text-sm text-gray-700" do %>
                <span class="font-medium">3日後に通知</span>
                <span class="text-gray-500 block text-xs mt-0.5">3日後に復習のリマインドを受け取ります</span>
              <% end %>
            </div>
            <div class="flex items-start">
              <%= f.check_box :notify_7days, class: "w-4 h-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500 mt-1" %>
              <%= f.label :notify_7days, class: "ml-3 text-sm text-gray-700" do %>
                <span class="font-medium">7日後に通知</span>
                <span class="text-gray-500 block text-xs mt-0.5">1週間後に復習のリマインドを受け取ります</span>
              <% end %>
            </div>
          </div>
          <p class="text-xs text-gray-500 mt-3 flex items-center gap-1">
            <svg class="w-4 h-4 inline text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            エビングハウスの忘却曲線に基づいた復習タイミングで知識を定着させます
          </p>
        </div>

        <!-- アクションボタン -->
        <div class="flex gap-3 pt-6 border-t">
          <%= link_to "キャンセル", knowledges_path, 
              class: "flex-1 px-6 py-3 border-2 border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 font-medium transition-colors text-center" %>
          <%= f.submit "ナレッジを保存", 
              class: "flex-1 px-6 py-3 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 font-medium shadow-md transition-colors" %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- ヒント -->
  <div class="mt-6 mb-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex items-start gap-3">
      <svg class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <div>
        <h4 class="text-sm font-semibold text-blue-900 mb-1">💡 ナレッジ作成のヒント</h4>
        <ul class="text-sm text-blue-800 space-y-1">
          <li>• コードスニペットは内容欄にそのまま貼り付けられます</li>
          <li>• 後で検索しやすいように、具体的なタイトルをつけましょう</li>
          <li>• 複数の参考URLを追加できます</li>
          <li>• Myタグから関連するタグを選択してください</li>
          <li>• 通知設定で自動的に復習のタイミングをお知らせします</li>
        </ul>
      </div>
    </div>
  </div>
</div>