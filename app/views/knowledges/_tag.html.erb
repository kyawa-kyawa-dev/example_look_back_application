<button type="button" class="px-4 py-2 rounded-full font-medium text-sm transition-all opacity-70 hover:opacity-100"
    style="background-color: <%= tag.color %>20; color: <%= tag.color %>; border-color: <%= tag.color %>40; border: 1px solid;">
  <span class="flex items-center gap-2">
    <span><%= "/##{tag.name}" %></span>
  </span>
</button>